import React from 'react'
import PropTypes from 'prop-types'

import BlogReadMoreContent from './BlogReadMoreContent'

const BlogReadMore = (props) => {
  return (
<>
<BlogReadMoreContent/>
</>
  )
}

export default BlogReadMore
